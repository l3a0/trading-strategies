# Python
__pycache__/
*.pyc
*.pyo

# Virtual environments
.venv/
venv/

# Test/coverage artifacts
.pytest_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# IDE / editor settings
.vscode/

# Claude Code local settings
.claude/settings.local.json
*_option_dailies.csv
*_option_dailies.csv.gz
# backfill datasets (release assets, e.g. msft_option_dailies_2008_2016.csv[.gz])
# (the *_option_dailies_full.csv[.gz] full-chain archives ride these patterns too)
*_option_dailies_*.csv
*_option_dailies_*.csv.gz
# 1-min intraday OHLCV archives (personal backups — OneDrive + sidecar sha256,
# not release assets; see the cold-storage README)
*_intraday_1min.csv
*_intraday_1min.csv.gz

# Trend-gate experiment checkpoints (result-bearing JSONL; resumable engine
# runs — ignored so resumes don't trip trend_gate.py's clean-tree guard)
trend_gate_runs/

# Edge-search ledger (generated, append-only; the campaign is pinned by
# test_edge_search.py and narrated in docs/edge_search.md — regenerable via
# `python edge_search.py`)
edge_ledger.jsonl
# NOTE: idea_ledger.jsonl (the committed LIFETIME trial ledger, interlock #3a) is
# the deliberate opposite — it is TRACKED, not ignored (deduped + deterministic,
# so it does not churn). Do not add it here. The same holds for gen_ledger.jsonl,
# its generative twin (design A; the audit log of the generative search, pinned by
# test_generative_search.py and narrated in docs/generative_search.md) — TRACKED.
# Factor proposer artifacts (factor_proposer.py / factor_search.py) — ignored for now:
# the factor search runs on a SYNTHETIC panel only (no real equity panel yet), so a run
# records a synthetic-data ledger/provenance, not a real exploration. When a real panel
# arrives, decide whether factor_ledger.jsonl is TRACKED (like idea_ledger) or ephemeral,
# and update here. The provenance audit log stays ignored regardless.
factor_ledger.jsonl
factor_proposal_provenance.jsonl

# Transient fetch-loop progress logs (download_option_dailies.py wrapper output)
fetch_progress.log
gld_tlt_fetch.log
xle_eem_fetch.log
batch_fetch.log
# S&P 500 1-min archive workspace (personal backups; list + state + csvs)
data/sp500_intraday_1min/
*_intraday_1min_daily_cache.csv
# Intraday-continuation scan output (regenerable: python -m engine.intraday_continuation --scan)
data/intraday_continuation_scan/
data/intraday_capture_scan/
