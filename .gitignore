# Python
__pycache__/
*.pyc
*.pyo

# Virtual environments
.venv/
venv/

# Test/coverage artifacts
.pytest_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# IDE / editor settings
.vscode/

# Claude Code local settings
.claude/settings.local.json
*_option_dailies.csv
*_option_dailies.csv.gz
# backfill datasets (release assets, e.g. msft_option_dailies_2008_2016.csv[.gz])
*_option_dailies_*.csv
*_option_dailies_*.csv.gz

# Trend-gate experiment checkpoints (result-bearing JSONL; resumable engine
# runs — ignored so resumes don't trip trend_gate.py's clean-tree guard)
trend_gate_runs/

# Edge-search ledger (generated, append-only; the campaign is pinned by
# test_edge_search.py and narrated in docs/edge_search.md — regenerable via
# `python edge_search.py`)
edge_ledger.jsonl
# NOTE: idea_ledger.jsonl (the committed LIFETIME trial ledger, interlock #3a) is
# the deliberate opposite — it is TRACKED, not ignored (deduped + deterministic,
# so it does not churn). Do not add it here.

# Transient fetch-loop progress logs (download_option_dailies.py wrapper output)
fetch_progress.log
gld_tlt_fetch.log
xle_eem_fetch.log
batch_fetch.log
